/**
 * @format
 */

import {AppRegistry} from 'react-native';
import StockTradingAssist from './App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => StockTradingAssist);
